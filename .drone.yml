kind: pipeline
type: docker
name: build
platform:
  os: linux
  arch: amd64
steps:
  -  name: ZenSpace Build ✨
     image: openjdk:jdk-slim
     commands:
       - cd ./packages/server
       - ./gradlew build --no-daemon
  -  name: ZenSpace Test 🗒️     
     image: openjdk:jdk-slim
     commands: 
       - cd ./packages/server
       - ./gradlew check
  -  name: ZenSpace Run ✅
     image: gradle:jdk17
     commands:
       - cd ./packages/server
       - ./gradlew run --no-daemon
